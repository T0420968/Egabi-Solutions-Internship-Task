spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/postgres
    username: postgres
    password: "Ahmedemam1235*"

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true